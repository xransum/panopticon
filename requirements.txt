# Core UI
PyQt6>=6.6.0

# Screen capture
mss>=9.0.1

# AI inference
ultralytics>=8.0.0

# Deep learning / GPU (CUDA build must be installed separately - see README)
# torch>=2.2.0   # install via https://pytorch.org/get-started/locally/
# torchvision>=0.17.0

# Image processing
numpy>=1.24.0
opencv-python>=4.9.0
Pillow>=10.0.0

# Platform-specific window enumeration
# Linux:
python-xlib>=0.33; sys_platform == "linux"
# Windows:
pywin32>=306; sys_platform == "win32"
# macOS:
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"
